t"><div id="mobileFilters" class="filter-options"></div><button id="applyFilters" class="apply">Show homes</button></div></section><section id="congrats" class="congrats"><div class="congrats-card"><div class="congrats-emoji">🎉💼✨</div><h2>CONGRATS TESS ON YOUR NEW JOB!!</h2><p>The house hunt officially has a bigger budget and a promotion-worthy amount of confetti.</p><button id="woohoo" class="woohoo">WOOHOO!</button></div></section>
`;

(()=>{const D=window.RENTAL_DATA||{properties:[]},SET='kt-house-settings-v4',SAVE='kt-saved',CMP='kt-compare',CON='kt-tess-new-job-congrats-v1';const defaults={kevin:D.incomes?.kevin||1730.97,tess:D.incomes?.tess||1620,comfort:1400,stretch:1500,split:'equal',carKevin:150,carTess:25,reserveKevin:125,reserveTess:125,leftKevin:500,leftTess:''};let prefs={...defaults,...json(localStorage.getItem(SET),{})},saved=json(localStorage.getItem(SAVE),[]),compared=json(localStorage.getItem(CMP),[]);const state={view:'top',filter:'all',sort:'personal',search:'',page:1,mobileShown:10};const views=[['top','🏆 Top 10'],['all','🏡 All '+(D.targetCount||50)],['saved','💖 Saved'],['compare','⚖️ Compare']];const filters=[['all','✨ All'],['underComfort','💷 Under comfort rent'],['garden','🌿 Outdoor space'],['parking2','🚗 Two-car friendly'],['guildford','📍 Guildford'],['epcC','⚡ EPC C+'],['photo','📸 Real photo'],['available','✅ Available']];const $=s=>document.querySelector(s),money=n=>'£'+Math.round(Number(n)||0).toLocaleString('en-GB'),json=(v,f)=>{try{return v?JSON.parse(v):f}catch{return f}},bill=p=>Object.values(p.bills||{}).reduce((a,b)=>a+(Number(b)||0),0),total=p=>Number(p.rent||0)+bill(p),isMobile=()=>matchMedia('(max-width:860px)').matches,dt=v=>{try{return new Intl.DateTimeFormat('en-GB',{day:'numeric',month:'short',year:'numeric'}).format(new Date(v))}catch{return 'Not recorded'}};
function split(x){if(prefs.split==='proportional'){const s=+prefs.kevin + +prefs.tess;return s?[x*prefs.kevin/s,x*prefs.tess/s]:[x/2,x/2]}return[x/2,x/2]}
function metrics(p){const [k,t]=split(total(p)),ka=+prefs.kevin-k-+prefs.carKevin-+prefs.reserveKevin,ta=+prefs.tess-t-+prefs.carTess-+prefs.reserveTess;return{k,t,ka,ta}}
function score(p){let s=Number(p.score||70),m=metrics(p);if(p.rent<=prefs.comfort)s+=4;else if(p.rent>prefs.stretch)s-=10;else s-=3;const lk=prefs.leftKevin===''?null:+prefs.leftKevin,lt=prefs.leftTess===''?null:+prefs.leftTess;if(lk!==null)s+=Math.max(-14,Math.min(6,(m.ka-lk)/50));if(lt!==null)s+=Math.max(-14,Math.min(6,(m.ta-lt)/50));s+=(p.parkingConfidence||0)*2+(p.outdoorConfidence||0)*1.5;if(['A','B','C'].includes(p.epc))s+=2;return Math.max(1,Math.min(99,Math.round(s)))}
function dedupe(list){const seen=new Set;return list.filter(p=>{const k=p.duplicateKey||`${p.name}|${p.area}|${p.rent}`.toLowerCase();if(seen.has(k))return false;seen.add(k);return true})}
function filtered(){let list=dedupe([...D.properties]);const q=state.search.trim().toLowerCase();if(q)list=list.filter(p=>[p.name,p.area,p.source,p.summary,p.parking,p.garden,p.guildfordAccess,...(p.pros||[])].join(' ').toLowerCase().includes(q));if(state.filter!=='all')list=list.filter(p=>{if(state.filter==='underComfort')return p.rent<=prefs.comfort;if(state.filter==='photo')return !!p.image;if(state.filter==='available')return /available/i.test(p.status||'');return (p.tags||[]).includes(state.filter)});if(state.view==='saved')list=list.filter(p=>saved.includes(p.duplicateKey||p.name));if(state.view==='compare')list=list.filter(p=>compared.includes(p.duplicateKey||p.name));if(state.sort==='rent')list.sort((a,b)=>a.rent-b.rent);else if(state.sort==='total')list.sort((a,b)=>total(a)-total(b));else if(state.sort==='distance')list.sort((a,b)=>(a.distanceMiles??999)-(b.distanceMiles??999));else if(state.sort==='verified')list.sort((a,b)=>new Date(b.verifiedAt||0)-new Date(a.verifiedAt||0));else if(state.sort==='rank')list.sort((a,b)=>a.rank-b.rank);else list.sort((a,b)=>score(b)-score(a)||a.rank-b.rank);if(state.view==='top')list=list.slice(0,10);return list}
function renderShell(){const count=D.properties.length;$('#landingHeadline').textContent=D.headline||'I have searched far and wide for a house for Tess & Kevin...';$('#updatedLabel').textContent='Checked '+dt(D.updated);$('#recommendation').textContent=D.recommendation||'The shortlist is being refreshed.';$('#incomeStat').textContent='💷 Combined take-home ≈ '+money(+prefs.kevin + +prefs.tess);$('#comfortStat').textContent='🧘 Comfort rent ≤ '+money(prefs.comfort);$('#countStat').textContent=`🏘️ ${count} verified · ${D.targetCount||50} target`;$('#targetPill').textContent=`${count}/${D.targetCount||50} verified`;$('#viewTabs').innerHTML=views.map(v=>`<button class="view-tab ${state.view===v[0]?'active':''}" data-view="${v[0]}">${v[1]}</button>`).join('');$('#bottomNav').innerHTML=views.map(v=>`<button class="nav ${state.view===v[0]?'active':''}" data-view="${v[0]}"><span>${v[1].split(' ')[0]}</span>${v[1].split(' ').slice(1).join(' ')}</button>`).join('');const fh=filters.map(f=>`<b