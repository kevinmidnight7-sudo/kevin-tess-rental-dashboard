oin('')}</tr></thead><tbody>${rows.map(r=>`<tr><td>${r[0]}</td>${homes.map(p=>`<td>${r[1](p)}</td>`).join('')}</tr>`).join('')}</tbody></table></div></div>`}
function bindCards(){document.querySelectorAll('[data-save]').forEach(b=>b.onclick=e=>{e.stopPropagation();toggle(saved,b.dataset.save,SAVE);render()});document.querySelectorAll('[data-compare]').forEach(b=>b.onclick=()=>{if(!compared.includes(b.dataset.compare)&&compared.length>=4){alert('Compare supports four homes at a time. Remove one first.');return}toggle(compared,b.dataset.compare,CMP);render()});document.querySelectorAll('[data-detail]').forEach(b=>b.onclick=()=>openDetail(+b.dataset.detail))}
function toggle(arr,key,store){const i=arr.indexOf(key);i>=0?arr.splice(i,1):arr.push(key);localStorage.setItem(store,JSON.stringify(arr))}
function openDetail(rank){const p=D.properties.find(x=>x.rank===rank);if(!p)return;const m=metrics(p),b=p.bills||{},sc=score(p),img=p.image?`<img src="${p.image}" alt="${p.name}" loading="lazy">`:`<a class="photo-help" href="${p.link}" target="_blank">View real listing photos ↗</a>`;$('#detailSheet').innerHTML=`<button class="sheet-close" data-close>×</button><div class="detail-hero">${img}<div class="detail-title"><h2>${p.name}</h2><p>${p.area} · ${money(p.rent)} pcm · ${p.source||'Agent'}</p></div></div><div class="detail-body"><div class="detail-ai"><b>✨ Why it scores ${sc}/99 for you</b><br>${p.summary}<br><br><b>Guildford:</b> ${p.guildfordAccess||'To confirm'} · <b>Parking confidence:</b> ${Math.round((p.parkingConfidence||0)*100)}% · <b>Outdoor confidence:</b> ${Math.round((p.outdoorConfidence||0)*100)}%</div><div class="breakdown"><div class="break"><span>🏠 Rent</span><b>${money(p.rent)}</b></div><div class="break"><span>🏛️ Council tax · Band ${p.councilBand||'TBC'}</span><b>${money(b.councilTax)}</b></div><div class="break"><span>⚡ Energy · EPC ${p.epc||'TBC'}</span><b>${money(b.energy)}</b></div><div class="break"><span>💧 Water</span><b>${money(b.water)}</b></div><div class="break"><span>📶 Broadband</span><b>${money(b.broadband)}</b></div><div class="break"><span>🛋️ Contents cover</span><b>${money(b.contents)}</b></div><div class="break total"><span>Estimated monthly total</span><b>${money(total(p))}</b></div></div><div class="split"><div class="person"><span>Kevin</span><b>${money(m.k)} housing share</b><small>${money(m.ka)} left after housing and car costs${prefs.leftKevin!==''?` · target ${money(prefs.leftKevin)}`:''}</small></div><div class="person"><span>Tess</span><b>${money(m.t)} housing share</b><small>${money(m.ta)} left after housing and car costs${prefs.leftTess!==''?` · target ${money(prefs.leftTess)}`:' · no target set'}</small></div></div><div class="lists"><div class="list pro"><b>Reasons to like it</b><ul>${(p.pros||[]).map(x=>`<li>${x}</li>`).join('')}</ul></div><div class="list con"><b>Things to check</b><ul>${(p.cons||[]).map(x=>`<li>${x}</li>`).join('')}</ul></div></div><p class="verified">Last verified ${dt(p.verifiedAt)} · ${p.availabilityStatus||p.status} · duplicate key ${p.duplicateKey||'generated'}</p><a class="listing" href="${p.link}" target="_blank" rel="noopener">Open original ${p.source||'agent'} listing ↗</a></div>`;openSheet('#detailSheet');document.querySelectorAll('[data-close]').forEach(b=>b.onclick=closeSheets)}
function openSheet(sel){$('#overlay').classList.add('open');$(sel).classList.add('open');document.body.classList.add('lock')}function closeSheets(){document.querySelectorAll('.sheet.open').forEach(x=>x.classList.remove('open'));$('#overlay').classList.remove('open');document.body.classList.remove('lock')}
function settings(open=true){if(open){const map={setKevin:'kevin',setTess:'tess',setComfort:'comfort',setStretch:'stretch',setSplit:'split',setCarKevin:'carKevin',setCarTess:'carTess',setReserveKevin:'reserveKevin',setReserveTess:'reserveTess',setLeftKevin:'leftKevin',setLeftTess:'leftTess'};Object.entries(map).forEach(([id,k])=>$('#'+id).value=prefs[k]);openSheet('#settingsSheet')}}
function savePrefs(){const val=id=>$('#'+id).value.trim(),num=id=>Math.max(0,Number(val(id))||0);prefs={kevin:num('setKevin'),tess:num('setTess'),comfort:num('setComfort'),stretch:num('setStretch'),split:val('setSplit'),carKevin:num('setCarKevin'),carTess:num('setCarTess'),reserveKevin:num('setReserveKevin'),reserveTess:num('setReserveTess'),leftKevin:val('setLeftKevin')===''?'':num('setLeftKevin'),leftTess:val('setLeftTess')===''?'':num('setLeftTess')};localStorage.setItem(SET,JSON.stringify(prefs));state.page=1;closeSheets();render()}
function resetPrefs(){prefs={...defaults};localStorage.removeItem(SET);settings();render()}
function esc(s){return String(s).replaceAll('&','&amp;').replaceAll('"','&quot;').replaceAll("'",'&#39;')}
function init(){render();$('#enterBtn').onclick=()=>{$('#landing').classList.add('gone');document.body.classList.remove('lock');setTimeout(()=>$('#landing').style.display='none',650);if(!localStorage.getItem(CON))setTimeout(()=>{$('#congrats').classList.ad