utton class="filter ${state.filter===f[0]?'active':''}" data-filter="${f[0]}">${f[1]}</button>`).join('');$('#filterChips').innerHTML=fh;$('#mobileFilters').innerHTML=filters.map(f=>`<button class="filter-option ${state.filter===f[0]?'active':''}" data-filter="${f[0]}">${f[1]}</button>`).join('');bindDynamic()}
function bindDynamic(){document.querySelectorAll('[data-view]').forEach(b=>b.onclick=()=>{state.view=b.dataset.view;state.page=1;state.mobileShown=10;render()});document.querySelectorAll('[data-filter]').forEach(b=>b.onclick=()=>{state.filter=b.dataset.filter;state.page=1;state.mobileShown=10;render()})}
function card(p,i){const m=metrics(p),key=p.duplicateKey||p.name,sv=saved.includes(key),cp=compared.includes(key),sc=score(p),has=!!p.image;return `<article class="card ${i<10?'top':''}" style="animation-delay:${Math.min(i,8)*45}ms"><div class="photo ${has?'':'no-photo'}">${has?`<img src="${p.image}" alt="${p.name} property" loading="lazy" onerror="this.remove();this.parentElement.classList.add('no-photo')">`:`<a class="photo-help" href="${p.link}" target="_blank" rel="noopener">View real listing photos ↗</a>`}<span class="rank">#${p.rank} · ${p.label||'Worth a look'}</span><span class="source">${p.source||'Agent'}</span><span class="status">${p.status||'Check status'}</span><button class="heart ${sv?'saved':''}" data-save="${esc(key)}">${sv?'♥':'♡'}</button><div class="photo-copy"><h4>${p.name}</h4><p>${p.area}</p></div></div><div class="body"><div class="card-top"><div><div class="rent">${money(p.rent)} <small>pcm</small></div><div class="allin">About ${money(total(p))} all-in monthly</div></div><div class="score"><b>${sc}</b><span>your score</span></div></div><div class="badges"><span class="badge ${['A','B','C'].includes(p.epc)?'good':'sun'}">⚡ EPC ${p.epc||'TBC'}</span><span class="badge pink">🚗 ${p.parking}</span><span class="badge">🌿 ${p.garden}</span><span class="badge">📍 ${p.guildfordAccess||'Access TBC'}</span></div><p class="summary">${p.summary}</p><div class="money"><div><span>Kevin after home + car</span><b>${money(m.ka)}</b></div><div><span>Tess after home + car</span><b>${money(m.ta)}</b></div><div><span>Verified</span><b>${dt(p.verifiedAt)}</b></div></div><div class="card-actions"><button class="details" data-detail="${p.rank}">Full cost recipe 🧾</button><button class="compare-btn ${cp?'active':''}" data-compare="${esc(key)}">${cp?'✓ Compare':'＋ Compare'}</button></div><p class="verified">${p.source||'Agent'} · ${p.availabilityStatus||p.status||'Status to confirm'} · duplicate key checked</p></div></article>`}
function render(){renderShell();const list=filtered();let title=state.view==='top'?'Top 10 personalised picks':state.view==='all'?`All ${list.length} matching homes`:state.view==='saved'?`${list.length} saved homes`:`Compare ${list.length} homes`;$('#resultTitle').textContent=title;$('#resultSub').textContent=`Search, sort and filters run across all ${D.properties.length} verified records before this view is paginated.`;if(state.view==='compare'){renderCompare(list);return}const per=10,totalPages=Math.max(1,Math.ceil(list.length/per));state.page=Math.min(state.page,totalPages);const shown=isMobile()?list.slice(0,state.mobileShown):list.slice((state.page-1)*per,state.page*per);$('#content').innerHTML=shown.length?`<div class="grid">${shown.map(card).join('')}</div>`:`<div class="empty"><div>🫠</div><h4>No homes match that combo</h4><p>Try relaxing a filter. The housing market is already dramatic enough.</p></div>`;$('#pagination').innerHTML=!isMobile()&&list.length>10?Array.from({length:totalPages},(_,i)=>`<button class="page-btn ${state.page===i+1?'active':''}" data-page="${i+1}">${i+1}</button>`).join(''):'';$('#loadMore').classList.toggle('hidden',!isMobile()||shown.length>=list.length||state.view==='top');$('#loadMore').textContent=`Load ${Math.min(10,list.length-shown.length)} more homes 🏡`;bindCards();document.querySelectorAll('[data-page]').forEach(b=>b.onclick=()=>{state.page=+b.dataset.page;render();scrollTo({top:$('#content').offsetTop-95,behavior:'smooth'})})}
function renderCompare(list){$('#pagination').innerHTML='';$('#loadMore').classList.add('hidden');if(!list.length){$('#content').innerHTML=`<div class="compare-empty"><div style="font-size:55px">⚖️</div><h3>Nothing selected yet</h3><p>Add up to four homes using the Compare button on any card.</p></div>`;return}const homes=list.slice(0,4),rows=[['Rent',p=>money(p.rent)],['All-in estimate',p=>money(total(p))],['Your score',p=>score(p)+'/99'],['Source',p=>p.source],['Status',p=>p.status],['Guildford access',p=>p.guildfordAccess],['Parking confidence',p=>Math.round((p.parkingConfidence||0)*100)+'%'],['Outdoor confidence',p=>Math.round((p.outdoorConfidence||0)*100)+'%'],['Kevin after home + car',p=>money(metrics(p).ka)],['Tess after home + car',p=>money(metrics(p).ta)]];$('#content').innerHTML=`<div class="compare-panel"><div class="compare-table"><table><thead><tr><th>Factor</th>${homes.map(p=>`<th>${p.name}</th>`).j